<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistem Inventaris & POS Multi-Halaman</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <style>
        @keyframes putarLambat {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        .animasi-gelombang {
            position: relative;
            overflow: hidden;
        }
        .animasi-gelombang::before {
            content: "";
            position: absolute;
            left: -50%;
            top: 60%;
            width: 200%;
            height: 200%;
            background: linear-gradient(180deg, rgba(255,255,255,0.15) 0%, rgba(16,185,129,0) 80%);
            border-radius: 40%;
            animation: putarLambat 12s linear infinite;
            pointer-events: none;
        }
    </style>
</head>
<body class="bg-slate-50 font-sans text-slate-800">

    <div class="flex min-h-screen">
        <aside class="w-64 bg-slate-900 text-slate-200 flex flex-col shrink-0 border-r border-slate-800 shadow-xl">
            <div class="p-6 border-b border-slate-800 bg-slate-950/40 flex items-center gap-3">
                <div class="w-8 h-8 bg-indigo-500 rounded-lg flex items-center justify-center font-bold text-white shadow-md shadow-indigo-500/20">M</div>
                <div>
                    <h1 class="font-bold text-white tracking-wide">MB-POS System</h1>
                    <p class="text-xs text-slate-400">Management & Cashier</p>
                </div>
            </div>

            <nav class="flex-1 p-4 space-y-1.5 overflow-y-auto">
                <button onclick="app.gantiHalaman('dashboard')" id="btn-dashboard" class="w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium text-sm bg-indigo-600 text-white shadow-lg shadow-indigo-600/10">
                    <span>📊</span> Dashboard Toko
                </button>
                <button onclick="app.gantiHalaman('kasir')" id="btn-kasir" class="w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium text-sm text-slate-400 hover:bg-slate-800/60 hover:text-slate-200">
                    <span>🛒</span> Menu Transaksi Kasir
                </button>
                <button onclick="app.gantiHalaman('laporan')" id="btn-laporan" class="w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium text-sm text-slate-400 hover:bg-slate-800/60 hover:text-slate-200">
                    <span>📜</span> Riwayat Pembukuan
                </button>
                <button onclick="app.gantiHalaman('gudang')" id="btn-gudang" class="w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium text-sm text-slate-400 hover:bg-slate-800/60 hover:text-slate-200">
                    <span>📦</span> Stok Gudang (Logistik)
                </button>
            </nav>

            <div class="p-4 border-t border-slate-800 bg-slate-950/20 flex items-center gap-3">
                <div class="w-9 h-9 bg-slate-700 rounded-full flex items-center justify-center font-bold text-sm text-white">KU</div>
                <div>
                    <h2 class="text-sm font-semibold text-white" id="sidebar-nama-kasir">Memuat...</h2>
                    <p class="text-xs text-indigo-400 font-medium">Administrator</p>
                </div>
            </div>
        </aside>

        <main class="flex-1 flex flex-col min-w-0 overflow-x-hidden">
            <header class="bg-white border-b border-slate-200 px-8 py-4 flex justify-between items-center shadow-sm">
                <div>
                    <h2 id="judul-halaman" class="text-xl font-bold text-slate-900">Dashboard Analisis Toko</h2>
                    <p class="text-xs text-slate-500">Pantau data penjualan, stok barang, dan profit tokomu secara real-time</p>
                </div>
                <div class="flex items-center gap-4 text-sm font-medium text-slate-600 bg-slate-100 px-4 py-2 rounded-xl">
                    <span>📅</span> <span id="waktu-sekarang">Memuat Waktu...</span>
                </div>
            </header>

            <div class="p-8 flex-1 space-y-8 overflow-y-auto max-w-7xl w-full mx-auto">

                <section id="halaman-dashboard" class="space-y-8 transition-all duration-300">
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm flex items-center gap-5">
                            <div class="w-12 h-12 rounded-xl bg-emerald-50 text-emerald-600 flex items-center justify-center text-xl font-bold">Rp</div>
                            <div>
                                <p class="text-xs font-semibold text-slate-400 uppercase tracking-wider">Total Pendapatan</p>
                                <h3 id="dash-omzet" class="text-2xl font-black text-slate-900 mt-1">Rp 0</h3>
                            </div>
                        </div>
                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm flex items-center gap-5">
                            <div class="w-12 h-12 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center text-xl">📦</div>
                            <div>
                                <p class="text-xs font-semibold text-slate-400 uppercase tracking-wider">Varian Produk (SKU)</p>
                                <h3 id="dash-sku" class="text-2xl font-black text-slate-900 mt-1">0 Item</h3>
                            </div>
                        </div>
                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm flex items-center gap-5">
                            <div class="w-12 h-12 rounded-xl bg-amber-50 text-amber-600 flex items-center justify-center text-xl">📊</div>
                            <div>
                                <p class="text-xs font-semibold text-slate-400 uppercase tracking-wider">Total Stok Fisik</p>
                                <h3 id="dash-stok" class="text-2xl font-black text-slate-900 mt-1">0 Pcs</h3>
                            </div>
                        </div>
                        <div class="bg-gradient-to-br from-indigo-600 to-violet-700 p-6 rounded-2xl text-white shadow-lg shadow-indigo-600/10 flex items-center gap-5 animasi-gelombang">
                            <div class="w-12 h-12 rounded-xl bg-white/10 text-white flex items-center justify-center text-xl relative z-10">👤</div>
                            <div class="relative z-10">
                                <p class="text-xs font-medium text-indigo-200 uppercase tracking-wider" id="dash-nama-penjual">Kasir Utama</p>
                                <h3 id="dash-transaksi-penjual" class="text-xl font-bold mt-0.5">0 Transaksi</h3>
                                <p id="dash-omzet-penjual" class="text-xs text-indigo-100/80 mt-0.5">Omzet: Rp 0</p>
                            </div>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm lg:col-span-2 flex flex-col">
                            <div class="flex justify-between items-center mb-6">
                                <h4 class="font-bold text-slate-900">Kurva Tren Performa Keuangan</h4>
                                <span class="text-xs bg-slate-100 text-slate-600 px-3 py-1 rounded-full font-medium">Bulan Berjalan</span>
                            </div>
                            <div class="relative flex-1 min-h-[320px]">
                                <canvas id="chartPendapatanBulanan"></canvas>
                            </div>
                        </div>
                        <div class="space-y-6">
                            <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm flex flex-col">
                                <h4 class="font-bold text-slate-900 mb-4">Proporsi Produk Terjual</h4>
                                <div class="relative w-full aspect-square max-w-[200px] mx-auto">
                                    <canvas id="chartProduk"></canvas>
                                </div>
                            </div>
                            <div class="bg-rose-50 border border-rose-100 p-5 rounded-2xl">
                                <div class="flex items-center gap-2.5 text-rose-700 font-bold mb-3 text-sm">
                                    <span>⚠️</span> Perhatian: Stok Kritis (&lt; 15 Pcs)
                                </div>
                                <ul id="list-stok-kritis" class="space-y-2 text-xs font-medium text-rose-600"></ul>
                            </div>
                        </div>
                    </div>
                </section>

                <section id="halaman-kasir" class="hidden space-y-8 transition-all duration-300">
                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                        
                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm lg:col-span-1 space-y-5">
                            <h4 class="font-bold text-slate-900 border-b border-slate-100 pb-3">Formulir Input Kasir</h4>
                            <div>
                                <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Pilih Item Menu</label>
                                <select id="kasir-id-barang" onchange="app.updateInfoHargaKasir()" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-medium text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all"></select>
                            </div>
                            <div class="grid grid-cols-2 gap-4 bg-slate-50 p-4 rounded-xl text-center border border-slate-100">
                                <div>
                                    <p class="text-[10px] font-bold text-slate-400 uppercase">Harga Satuan</p>
                                    <p id="kasir-harga-satuan" class="text-sm font-black text-indigo-600 mt-0.5">Rp 0</p>
                                </div>
                                <div>
                                    <p class="text-[10px] font-bold text-slate-400 uppercase">Sisa Stok Fisik</p>
                                    <p id="kasir-stok-tersisa" class="text-sm font-black text-slate-700 mt-0.5">0 Pcs</p>
                                </div>
                            </div>
                            <div>
                                <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Jumlah Beli (Qty)</label>
                                <input type="number" id="kasir-jumlah" value="1" min="1" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-semibold text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all">
                            </div>
                            
                            <button onclick="app.tambahKeKeranjang()" class="w-full bg-slate-800 hover:bg-slate-900 text-white font-bold text-sm py-2.5 rounded-xl transition-all shadow-md flex items-center justify-center gap-2 cursor-pointer">
                                <span>📥</span> Masukkan Ke Keranjang
                            </button>

                            <div class="border-t border-slate-100 my-2"></div>
                            
                            <div class="flex justify-between items-center bg-indigo-50/50 px-4 py-3 rounded-xl border border-indigo-100/50">
                                <span class="text-xs font-bold text-indigo-900 uppercase">Total Tagihan</span>
                                <span id="kasir-total-harga" class="text-lg font-black text-indigo-700">Rp 0</span>
                            </div>
                            <div>
                                <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Uang Fisik Diterima</label>
                                <input type="number" id="kasir-uang-bayar" placeholder="Contoh: 50000" oninput="app.hitungKembalianKasir()" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-semibold text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all">
                            </div>
                            <div class="flex justify-between items-center px-4 py-2">
                                <span class="text-xs font-semibold text-slate-500 uppercase">Kembalian Uang</span>
                                <span id="kasir-kembalian" class="text-base font-bold text-slate-900">Rp 0</span>
                            </div>
                            
                            <div class="grid grid-cols-2 gap-3">
                                <button onclick="app.eksekusiTransaksiBeli()" class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-xs py-3.5 rounded-xl transition-all shadow-lg flex items-center justify-center gap-1.5 cursor-pointer">
                                    <span>🔒</span> Kunci Transaksi
                                </button>
                                <button onclick="app.resetSistemKasirDanStruk()" class="bg-rose-100 hover:bg-rose-200 text-rose-700 font-bold text-xs py-3.5 rounded-xl transition-all flex items-center justify-center gap-1.5 cursor-pointer">
                                    <span>🔄</span> Reset Form & Struk
                                </button>
                            </div>
                        </div>

                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm lg:col-span-2 space-y-6">
                            <div>
                                <h4 class="font-bold text-slate-900 mb-3 text-sm flex items-center gap-2"><span>🛒</span> Daftar Keranjang Belanja Sementara</h4>
                                <div class="border border-slate-200 rounded-xl overflow-hidden text-xs">
                                    <table class="w-full text-left">
                                        <thead class="bg-slate-100 font-bold text-slate-600 border-b border-slate-200">
                                            <tr>
                                                <th class="p-3">Nama Produk</th>
                                                <th class="p-3 text-center">Qty</th>
                                                <th class="p-3 text-right">Harga Satuan</th>
                                                <th class="p-3 text-right">Subtotal</th>
                                                <th class="p-3 text-center">Aksi</th>
                                            </tr>
                                        </thead>
                                        <tbody id="tabel-keranjang-body" class="divide-y divide-slate-100 font-medium text-slate-600">
                                            <tr>
                                                <td colspan="5" class="p-4 text-center text-slate-400 italic">Belum ada barang di keranjang. Silakan tambah barang di sebelah kiri!</td>
                                            </tr>
                                        </tbody>
                                    </table>
                                </div>
                            </div>

                            <h4 class="font-bold text-slate-900 border-b border-slate-100 pb-2 text-sm">Struk Transaksi Aktif</h4>
                            <div class="max-w-md bg-slate-50 p-6 rounded-2xl border border-slate-200/60 shadow-inner font-mono text-xs mx-auto">
                                <div class="text-center space-y-1 border-b border-dashed border-slate-300 pb-4">
                                    <h5 class="font-bold text-sm text-slate-900">WARUNG MB-POS SYSTEM</h5>
                                    <p class="text-slate-400">Jl. Kewirausahaan Mahasiswa No. 06</p>
                                </div>
                                <div class="space-y-1.5 py-3 text-slate-600 border-b border-dashed border-slate-300">
                                    <div class="flex justify-between"><span>Waktu Cetak:</span><span id="nota-waktu">-</span></div>
                                    <div class="flex justify-between"><span>Petugas:</span><span id="nota-kasir" class="font-bold text-slate-700">-</span></div>
                                </div>
                                
                                <div id="nota-list-items" class="py-3 space-y-2 text-slate-700">
                                    <div class="text-center text-slate-400 italic">Nota kosong</div>
                                </div>
                                
                                <div class="border-t border-dashed border-slate-300 pt-3 space-y-1.5 font-bold text-slate-900">
                                    <div class="flex justify-between text-sm"><span>TOTAL AKHIR:</span><span id="nota-total" class="text-indigo-600">Rp 0</span></div>
                                    <div class="flex justify-between font-normal text-slate-600"><span>Tunai Diterima:</span><span id="nota-bayar">Rp 0</span></div>
                                    <div class="flex justify-between text-sm border-t border-slate-200 pt-1.5 text-emerald-600"><span>KEMBALIAN:</span><span id="nota-kembalian">Rp 0</span></div>
                                </div>
                                <p class="text-center text-[10px] text-slate-400 italic pt-4">Terima kasih telah berbelanja di toko kami!</p>
                            </div>
                        </div>
                    </div>
                </section>

                <section id="halaman-laporan" class="hidden space-y-6 transition-all duration-300">
                    <div class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden flex flex-col">
                        <div class="p-6 border-b border-slate-100 flex flex-col sm:flex-row justify-between sm:items-center gap-4 bg-slate-50/50">
                            <h4 class="font-bold text-slate-900">Jurnal Riwayat Buku Kasir</h4>
                            <span id="log-total-baris" class="text-xs font-semibold text-indigo-600 bg-indigo-50 border border-indigo-100 px-3 py-1 rounded-full">Total: 0 Entri Transaksi</span>
                        </div>
                        <div class="overflow-x-auto min-w-full">
                            <table class="w-full text-left border-collapse">
                                <thead>
                                    <tr class="border-b border-slate-200 bg-slate-100/80 text-slate-500 font-bold text-xs uppercase tracking-wider">
                                        <th class="px-6 py-3.5 font-bold">Jam Kerja</th>
                                        <th class="px-6 py-3.5 font-bold">Deskripsi Produk</th>
                                        <th class="px-6 py-3.5 text-center font-bold">Kuantitas</th>
                                        <th class="px-6 py-3.5 text-right font-bold">Omzet Penjualan</th>
                                        <th class="px-6 py-3.5 text-right font-bold">Uang Fisik</th>
                                        <th class="px-6 py-3.5 text-right font-bold">Kembalian</th>
                                        <th class="px-6 py-3.5 font-bold text-center">Petugas</th>
                                    </tr>
                                </thead>
                                <tbody id="tabel-laporan-body" class="divide-y divide-slate-100 text-sm font-medium text-slate-600"></tbody>
                            </table>
                        </div>
                    </div>
                </section>

                <section id="halaman-gudang" class="hidden space-y-8 transition-all duration-300">
                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm space-y-5">
                            <div class="flex items-center gap-3 border-b border-slate-100 pb-3">
                                <div class="w-8 h-8 rounded-lg bg-amber-50 text-amber-600 flex items-center justify-center font-bold">📥</div>
                                <h4 class="font-bold text-slate-900">Pasokan Stok Masuk (Restock Item)</h4>
                            </div>
                            <div>
                                <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Pilih Barang yang Datang</label>
                                <select id="gudang-id-barang" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-medium text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all"></select>
                            </div>
                            <div>
                                <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Jumlah Stok Ditambahkan</label>
                                <input type="number" id="gudang-jumlah-tambah" placeholder="Contoh: 20" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-semibold text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all">
                            </div>
                            <button onclick="app.eksekusiTambahStokGudang()" class="w-full bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-sm py-3.5 rounded-xl transition-all shadow-lg shadow-amber-500/10 flex items-center justify-center gap-2 cursor-pointer">
                                <span>➕</span> Suntik Tambah Stok Ke Gudang
                            </button>
                        </div>

                        <div class="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm space-y-5">
                            <div class="flex items-center gap-3 border-b border-slate-100 pb-3">
                                <div class="w-8 h-8 rounded-lg bg-indigo-50 text-indigo-600 flex items-center justify-center font-bold">✨</div>
                                <h4 class="font-bold text-slate-900">Registrasi Varian Produk Baru (SKU Baru)</h4>
                            </div>
                            <div>
                                <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Nama Menu / Produk Baru</label>
                                <input type="text" id="gudang-nama-baru" placeholder="Contoh: Aqua Gelas" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-medium text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all">
                            </div>
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Stok Awal Fisik</label>
                                    <input type="number" id="gudang-stok-baru" placeholder="Mulai dari: 10" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-semibold text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all">
                                </div>
                                <div>
                                    <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Harga Jual Per Unit</label>
                                    <input type="number" id="gudang-harga-baru" placeholder="Contoh: 1000" class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 font-semibold text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all">
                                </div>
                            </div>
                            <button onclick="app.eksekusiRegistrasiBarangBaru()" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-sm py-3.5 rounded-xl transition-all shadow-lg shadow-indigo-600/10 flex items-center justify-center gap-2 cursor-pointer">
                                <span>🚀</span> Daftarkan & Publikasi Produk Baru
                            </button>
                        </div>
                    </div>
                </section>

            </div>
        </main>
    </div>

    <script>
        class AplikasiKasir {
            constructor() {
                // Properti/Atribut Class Enkapsulasi data
                this.chartPendapatanBulananInstance = null;
                this.chartProdukInstance = null;
                this.globalBarangData = {};
                this.keranjangBelanja = []; 
            }

            // Method Navigasi Konten Halaman
            gantiHalaman(idHalaman) {
                const listHalaman = ['dashboard', 'kasir', 'laporan', 'gudang'];
                listHalaman.forEach(h => {
                    document.getElementById(`halaman-${h}`).classList.add('hidden');
                    document.getElementById(`btn-${h}`).className = "w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium text-sm text-slate-400 hover:bg-slate-800/60 hover:text-slate-200";
                });

                document.getElementById(`halaman-${idHalaman}`).classList.remove('hidden');
                document.getElementById(`btn-${idHalaman}`).className = "w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium text-sm bg-indigo-600 text-white shadow-lg shadow-indigo-600/10";

                const judulMap = {
                    'dashboard': 'Dashboard Analisis Toko',
                    'kasir': 'Menu Transaksi Kasir',
                    'laporan': 'Riwayat Pembukuan',
                    'gudang': 'Stok Gudang (Logistik)'
                };
                document.getElementById('judul-halaman').innerText = judulMap[idHalaman];
            }

            // Method Sinkronisasi Data Master Ke Dashboard Visual
            muatDataDashboard() {
                fetch('/api/dashboard-data')
                    .then(res => res.json())
                    .then(data => {
                        this.globalBarangData = data.barang;

                        document.getElementById('sidebar-nama-kasir').innerText = data.penjual.nama;
                        document.getElementById('dash-nama-penjual').innerText = data.penjual.nama;
                        document.getElementById('dash-omzet').innerText = "Rp " + data.total_pendapatan.toLocaleString('id-ID');
                        document.getElementById('dash-sku').innerText = Object.keys(data.barang).length + " Item";
                        document.getElementById('dash-transaksi-penjual').innerText = data.penjual.total_transaksi + " Transaksi";
                        document.getElementById('dash-omzet-penjual').innerText = "Omzet: Rp " + data.penjual.omzet_sekarang.toLocaleString('id-ID');

                        let totalStokFisik = 0;
                        let listStokKritisHtml = "";
                        let labelsProdukList = [];
                        let datasetTerjualList = [];

                        const selectKasir = document.getElementById('kasir-id-barang');
                        const selectGudang = document.getElementById('gudang-id-barang');
                        
                        const selectedKasirValue = selectKasir.value;
                        const selectedGudangValue = selectGudang.value;

                        selectKasir.innerHTML = "";
                        selectGudang.innerHTML = "";

                        for (const id in data.barang) {
                            const b = data.barang[id];
                            totalStokFisik += b.stok;

                            selectKasir.innerHTML += `<option value="${id}">${b.nama}</option>`;
                            selectGudang.innerHTML += `<option value="${id}">${b.nama}</option>`;

                            labelsProdukList.push(b.nama);
                            datasetTerjualList.push(b.terjual);

                            if (b.stok < 15) {
                                listStokKritisHtml += `<li class="flex justify-between bg-white px-3 py-1.5 rounded-lg border border-rose-100 shadow-xs"><span>📦 ${b.nama}</span> <span class="font-bold text-rose-700">${b.stok} Pcs</span></li>`;
                            }
                        }

                        if(selectedKasirValue) selectKasir.value = selectedKasirValue;
                        if(selectedGudangValue) selectGudang.value = selectedGudangValue;

                        document.getElementById('dash-stok').innerText = totalStokFisik + " Pcs";
                        document.getElementById('list-stok-kritis').innerHTML = listStokKritisHtml || `<li class="text-emerald-600 font-bold">✅ Semua stok aman melimpah!</li>`;

                        this.updateInfoHargaKasir();
                        this.renderGrafikLingkaran(labelsProdukList, datasetTerjualList);
                        this.renderGrafikLineKurva(data.chart_bulanan);
                        this.renderTabelLaporanJurnal(data.laporan);
                    });
            }

            // Method OOP Pengolah Chart Lingkaran
            renderGrafikLingkaran(labels, dataset) {
                const ctxProd = document.getElementById('chartProduk').getContext('2d');
                if(this.chartProdukInstance) this.chartProdukInstance.destroy();
                this.chartProdukInstance = new Chart(ctxProd, {
                    type: 'doughnut',
                    data: {
                        labels: labels,
                        datasets: [{
                            data: dataset,
                            backgroundColor: ['#6366f1', '#10b981', '#f59e0b', '#ec4899', '#8b5cf6', '#3b82f6'],
                            borderWidth: 2,
                            borderColor: '#ffffff'
                        }]
                    },
                    options: { responsive: true, maintainAspectRatio: false }
                });
            }

            // Method OOP Pengolah Chart Line
            renderGrafikLineKurva(chartBulananData) {
                const ctxPndp = document.getElementById('chartPendapatanBulanan').getContext('2d');
                if(this.chartPendapatanBulananInstance) this.chartPendapatanBulananInstance.destroy();
                this.chartPendapatanBulananInstance = new Chart(ctxPndp, {
                    type: 'line',
                    data: {
                        labels: chartBulananData.labels,
                        datasets: [
                            {
                                label: 'Total Pemasukan (Omzet)',
                                data: chartBulananData.pendapatan,
                                borderColor: '#6366f1',
                                borderWidth: 3,
                                fill: false,
                                tension: 0.3
                            },
                            {
                                label: 'Estimasi Keuntungan Bersih (Profit)',
                                data: chartBulananData.profit,
                                borderColor: '#10b981',
                                borderWidth: 3,
                                fill: false,
                                tension: 0.3
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: { callback: function(value) { return "Rp " + value.toLocaleString('id-ID'); } }
                            }
                        }
                    }
                });
            }

            // Method OOP Renderer Riwayat Jurnal
            renderTabelLaporanJurnal(laporanList) {
                const tbody = document.getElementById('tabel-laporan-body');
                tbody.innerHTML = "";
                document.getElementById('log-total-baris').innerText = `Total: ${laporanList.length} Entri Transaksi`;
                
                laporanList.slice().reverse().forEach(log => {
                    tbody.innerHTML += `
                        <tr class="hover:bg-slate-50 transition-colors">
                            <td class="px-6 py-4 font-mono font-bold text-slate-500">${log.waktu}</td>
                            <td class="px-6 py-4 font-bold text-slate-900">${log.nama_barang}</td>
                            <td class="px-6 py-4 text-center font-bold text-indigo-600">${log.jumlah} Pcs</td>
                            <td class="px-6 py-4 text-right font-black text-slate-900">Rp ${log.total_harga.toLocaleString('id-ID')}</td>
                            <td class="px-6 py-4 text-right text-slate-500">Rp ${log.uang_bayar.toLocaleString('id-ID')}</td>
                            <td class="px-6 py-4 text-right text-emerald-600 font-bold">Rp ${log.kembalian.toLocaleString('id-ID')}</td>
                            <td class="px-6 py-4 text-center"><span class="bg-slate-100 px-2.5 py-1 rounded-md text-xs font-bold text-slate-600">${log.penjual}</span></td>
                        </tr>
                    `;
                });
            }

            // Method Update Info UI Form Kasir
            updateInfoHargaKasir() {
                const idSelected = document.getElementById('kasir-id-barang').value;
                if (idSelected && this.globalBarangData[idSelected]) {
                    const b = this.globalBarangData[idSelected];
                    document.getElementById('kasir-harga-satuan').innerText = "Rp " + b.harga.toLocaleString('id-ID');
                    document.getElementById('kasir-stok-tersisa').innerText = b.stok + " Pcs";
                }
            }

            // Method Menambahkan Item Ke Array Keranjang Virtual
            tambahKeKeranjang() {
                const id_barang = document.getElementById('kasir-id-barang').value;
                const jumlah = parseInt(document.getElementById('kasir-jumlah').value) || 0;

                if (jumlah <= 0) {
                    alert("Jumlah beli minimal 1 unit!");
                    return;
                }

                const barang = this.globalBarangData[id_barang];
                if (jumlah > barang.stok) {
                    alert(`Gagal! Stok ${barang.nama} tidak mencukupi (Sisa: ${barang.stok} Pcs).`);
                    return;
                }

                const indexExist = this.keranjangBelanja.findIndex(item => item.id_barang === id_barang);
                if (indexExist >= 0) {
                    const totalQtyBaru = this.keranjangBelanja[indexExist].jumlah + jumlah;
                    if (totalQtyBaru > barang.stok) {
                        alert(`Gagal! Akumulasi kuantitas di keranjang melewati sisa stok fisik.`);
                        return;
                    }
                    this.keranjangBelanja[indexExist].jumlah = totalQtyBaru;
                } else {
                    this.keranjangBelanja.push({
                        id_barang: id_barang,
                        nama: barang.nama,
                        harga: barang.harga,
                        jumlah: jumlah
                    });
                }

                document.getElementById('kasir-jumlah').value = 1; 
                this.renderTabelKeranjangDanTotal();
            }

            // Method Menghapus Item dari Array Keranjang Virtual
            hapusItemKeranjang(index) {
                this.keranjangBelanja.splice(index, 1);
                this.renderTabelKeranjangDanTotal();
            }

            // Method Sinkronisasi Keranjang Virtual Ke UI HTML Table
            renderTabelKeranjangDanTotal() {
                const tbody = document.getElementById('tabel-keranjang-body');
                let totalTagihan = 0;

                if (this.keranjangBelanja.length === 0) {
                    tbody.innerHTML = `<tr><td colspan="5" class="p-4 text-center text-slate-400 italic">Belum ada barang di keranjang. Silakan tambah barang di sebelah kiri!</td></tr>`;
                    document.getElementById('kasir-total-harga').innerText = "Rp 0";
                    this.hitungKembalianKasir();
                    return;
                }

                tbody.innerHTML = "";
                this.keranjangBelanja.forEach((item, index) => {
                    const subtotal = item.harga * item.jumlah;
                    totalTagihan += subtotal;

                    tbody.innerHTML += `
                        <tr class="hover:bg-slate-50">
                            <td class="p-3 font-bold text-slate-900">${item.nama}</td>
                            <td class="p-3 text-center font-bold text-indigo-600">${item.jumlah} Pcs</td>
                            <td class="p-3 text-right">Rp ${item.harga.toLocaleString('id-ID')}</td>
                            <td class="p-3 text-right font-black text-slate-800">Rp ${subtotal.toLocaleString('id-ID')}</td>
                            <td class="p-3 text-center">
                                <button onclick="app.hapusItemKeranjang(${index})" class="bg-rose-50 hover:bg-rose-100 text-rose-600 font-bold px-2 py-1 rounded-lg transition-all cursor-pointer">Hapus</button>
                            </td>
                        </tr>
                    `;
                });

                document.getElementById('kasir-total-harga').innerText = "Rp " + totalTagihan.toLocaleString('id-ID');
                this.hitungKembalianKasir();
            }

            // Method Live Math Hitung Kembalian Uang Pembeli
            hitungKembalianKasir() {
                let totalTagihan = 0;
                this.keranjangBelanja.forEach(item => totalTagihan += (item.harga * item.jumlah));
                
                const uangBayar = parseInt(document.getElementById('kasir-uang-bayar').value) || 0;
                const kembalian = uangBayar - totalTagihan;
                document.getElementById('kasir-kembalian').innerText = "Rp " + (kembalian >= 0 ? kembalian.toLocaleString('id-ID') : 0);
            }

            // Method Komunikasi API POST: Kirim Paket Data Belanjaan Ke Backend Multi-Item
            eksekusiTransaksiBeli() {
                if (this.keranjangBelanja.length === 0) {
                    alert("Keranjang kasir kosong!");
                    return;
                }

                const uang_bayar = parseInt(document.getElementById('kasir-uang-bayar').value) || 0;
                let totalTagihan = 0;
                this.keranjangBelanja.forEach(item => totalTagihan += (item.harga * item.jumlah));

                if (uang_bayar < totalTagihan) {
                    alert(`Uang pembayaran kurang! Total tagihan: Rp ${totalTagihan.toLocaleString('id-ID')}`);
                    return;
                }

                // Hitung Payload Data Paket untuk dikirim ke Backend Multi-Item
                const payloadBelanja = this.keranjangBelanja.map(item => ({
                    id_barang: item.id_barang,
                    jumlah: item.jumlah
                }));

                fetch('/api/beli', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        daftar_belanja: payloadBelanja,
                        uang_bayar: uang_bayar
                    })
                })
                .then(res => res.json())
                .then(data => {
                    if (data.status === 'sukses') {
                        alert("Transaksi Sukses Dikunci! Mencetak Struk Nota.");
                        
                        // Render data struk aktif ke kertas digital
                        document.getElementById('nota-waktu').innerText = data.waktu;
                        document.getElementById('nota-kasir').innerText = document.getElementById('sidebar-nama-kasir').innerText;
                        
                        const notaListContainer = document.getElementById('nota-list-items');
                        notaListContainer.innerHTML = "";
                        
                        this.keranjangBelanja.forEach(item => {
                            const sub = item.harga * item.jumlah;
                            notaListContainer.innerHTML += `
                                <div class="space-y-0.5">
                                    <div class="flex justify-between font-bold text-slate-900"><span>${item.nama}</span><span>Rp ${sub.toLocaleString('id-ID')}</span></div>
                                    <div class="text-slate-400 text-[10px]">${item.jumlah} Pcs x Rp ${item.harga.toLocaleString('id-ID')}</div>
                                </div>
                            `;
                        });

                        document.getElementById('nota-total').innerText = "Rp " + totalTagihan.toLocaleString('id-ID');
                        document.getElementById('nota-bayar').innerText = "Rp " + uang_bayar.toLocaleString('id-ID');
                        document.getElementById('nota-kembalian').innerText = "Rp " + data.kembalian.toLocaleString('id-ID');

                        // AUTO-RESET: Bersihkan form input & data state internal
                        this.keranjangBelanja = [];
                        this.renderTabelKeranjangDanTotal();
                        document.getElementById('kasir-uang-bayar').value = "";
                        
                        // Tarik ulang data terbaru dari memori server
                        this.muatDataDashboard();
                    } else {
                        alert("Gagal Kunci Transaksi: " + data.pesan);
                    }
                });
            }

            // Method Manual Reset: Mengembalikan Kondisi Form Kasir & Nota ke Polosan
            resetSistemKasirDanStruk() {
                this.keranjangBelanja = [];
                this.renderTabelKeranjangDanTotal();
                
                document.getElementById('kasir-jumlah').value = 1;
                document.getElementById('kasir-uang-bayar').value = "";
                document.getElementById('kasir-kembalian').innerText = "Rp 0";
                
                document.getElementById('nota-waktu').innerText = "-";
                document.getElementById('nota-kasir').innerText = "-";
                document.getElementById('nota-total').innerText = "Rp 0";
                document.getElementById('nota-bayar').innerText = "Rp 0";
                document.getElementById('nota-kembalian').innerText = "Rp 0";
                document.getElementById('nota-list-items').innerHTML = `<div class="text-center text-slate-400 italic">Nota kosong</div>`;
                
                alert("Sistem POS kasir dan struk berhasil di-reset kembali bersih!");
            }

            // Method Komunikasi API POST: Tambah Stok Gudang
            eksekusiTambahStokGudang() {
                const id_barang = document.getElementById('gudang-id-barang').value;
                const jumlah = document.getElementById('gudang-jumlah-tambah').value;

                if(!jumlah || jumlah <= 0) {
                    alert("Masukkan jumlah stok valid!");
                    return;
                }

                fetch('/api/tambah-stok', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id_barang, jumlah })
                })
                .then(res => res.json())
                .then(data => {
                    if(data.status === 'sukses') {
                        alert("Pasokan Berhasil Disuntik Masuk ke Gudang!");
                        document.getElementById('gudang-jumlah-tambah').value = "";
                        this.muatDataDashboard();
                    } else {
                        alert("Gagal Tambah Stok!");
                    }
                });
            }

            // Method Komunikasi API POST: SKU Registrasi Barang Baru
            eksekusiRegistrasiBarangBaru() {
                const nama = document.getElementById('gudang-nama-baru').value;
                const stok = document.getElementById('gudang-stok-baru').value;
                const harga = document.getElementById('gudang-harga-baru').value;

                if(!nama || !stok || !harga || stok < 0 || harga <= 0) {
                    alert("Mohon isi formulir registrasi SKU barang baru dengan benar!");
                    return;
                }

                fetch('/api/barang-baru', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ nama, stok, harga })
                })
                .then(res => res.json())
                .then(data => {
                    if(data.status === 'sukses') {
                        alert(`Produk "${nama}" Sukses Ditambahkan ke Sistem OOP Toko!`);
                        document.getElementById('gudang-nama-baru').value = "";
                        document.getElementById('gudang-stok-baru').value = "";
                        document.getElementById('gudang-harga-baru').value = "";
                        this.muatDataDashboard();
                    } else {
                        alert("Gagal Mendaftarkan Barang Baru!");
                    }
                });
            }
        }

        // INSTANSIASI: Membuat Objek Tunggal 'app' Berdasarkan Blueprint Class Utama
        const app = new AplikasiKasir();

        // Event listener saat DOM browser selesai dimuat penuh
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('waktu-sekarang').innerText = new Date().toLocaleDateString('id-ID', {
                weekday: 'long', year: 'numeric', month: 'long', day: 'numeric'
            });
            app.muatDataDashboard();
        });
    </script>
</body>
</html>